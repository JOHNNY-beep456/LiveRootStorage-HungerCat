<Window
    x:Class="LRS.Views.MainWindowView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LRS.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:LRS.ViewModels"
    xmlns:views="using:LRS.Views"
	xmlns:CTKTCtrls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>
    <Grid AllowDrop="True"
          DragOver="OnRootDragOver"
          DragLeave="OnRootDragLeave"
          Drop="OnRootDrop">
        <Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
			<ColumnDefinition Width="10" />
			<ColumnDefinition Width="7*" />
        </Grid.ColumnDefinitions>
        <TitleBar x:Name="AppTitleBar"
                  Grid.ColumnSpan="3"
              Title="HungerCat LRS" Subtitle="0.0.3-beta">
            <TitleBar.IconSource>
				<FontIconSource Glyph="&#xE838;"/>
            </TitleBar.IconSource>
        </TitleBar>
		<views:TopView Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3"/>
		<views:FileTreeView Grid.Row="2" Grid.Column="0" />
		<CTKTCtrls:GridSplitter Grid.Row="2" Grid.Column="1" Grid.RowSpan="1"/>
		<Grid Grid.Row="2" Grid.Column="2">
			<views:MiddleFilesView x:Name="MiddleFilesPanel" />
			<views:SettingsView x:Name="SettingsPanelView" Visibility="Collapsed" />
			<views:SearchView x:Name="SearchPanelView" Visibility="Collapsed" />
		</Grid>

		<!-- 拖入扩展时的全屏覆盖层：默认隐藏，DragOver 时显示。放在 Grid 最后以保证绘制在最上层。-->
		<Border x:Name="DropOverlay"
		        Grid.RowSpan="3"
		        Grid.ColumnSpan="3"
		        Visibility="Collapsed"
		        IsHitTestVisible="False"
		        Background="{ThemeResource SystemControlBackgroundBaseLowBrush}"
		        Opacity="0.92"
		        BorderBrush="{ThemeResource SystemAccentColorLight2}"
		        BorderThickness="3">
			<StackPanel HorizontalAlignment="Center"
			            VerticalAlignment="Center"
			            Spacing="16">
				<FontIcon Glyph="&#xE8E5;"
				          FontSize="56"
				          Foreground="{ThemeResource SystemAccentColorLight2}" />
				<TextBlock Text="拖入 .hlds 文件以安装扩展"
				           FontSize="22"
				           FontWeight="SemiBold"
				           HorizontalAlignment="Center" />
				<TextBlock Text="支持同时拖入多个；松开后会自动重载"
				           FontSize="13"
				           Opacity="0.7"
				           HorizontalAlignment="Center" />
			</StackPanel>
		</Border>
    </Grid>

</Window>
